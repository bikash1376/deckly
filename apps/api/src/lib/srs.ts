/**
 * Re-exported from @deckly/shared. The app predicts the next interval with this
 * same function so its "next review in 4 days" hint matches what the server
 * actually writes.
 */
export { schedule, INITIAL_SRS, type SrsState } from "@deckly/shared";
