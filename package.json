{
  "name": "retenit",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "mobile": "pnpm --filter @retenit/mobile",
    "api": "pnpm --filter @retenit/api",
    "db:generate": "pnpm --filter @retenit/api db:generate",
    "db:migrate": "pnpm --filter @retenit/api db:migrate",
    "typecheck": "pnpm -r --parallel typecheck"
  },
  "packageManager": "pnpm@11.5.1"
}
