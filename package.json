{
  "name": "deckly",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "mobile": "pnpm --filter @deckly/mobile",
    "api": "pnpm --filter @deckly/api",
    "db:generate": "pnpm --filter @deckly/api db:generate",
    "db:migrate": "pnpm --filter @deckly/api db:migrate"
  },
  "packageManager": "pnpm@11.5.1"
}
